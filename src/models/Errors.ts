type Errors = {
    first_name?: string,
    last_name?: string,
    email?: string,
    phone?: string,
    password?: string,
    passwordConfirmation?: string,
    passwords?: string,
    creds?: string,
}